#include<stdio.h>
int fac(int n)
{
	if (n <= 1)
		return 1;
	else
		return n*fac(n - 1);
}
int fac(int n)
{
	int sum=1;
	if (n <= 1)
		return 1;
	else
	{
		for (int i = 1; i <= n; i++)
		{
			sum = sum*i;
		}
	}return sum;
}
int main()
{
	int c = fac(3);
	printf("%d\n", c);
	return 0;
}
