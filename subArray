class Solution {
    public List<List<Integer>> subsets(int[] nums) {
        List<List<Integer>> num=new ArrayList<>();
        num.add(new ArrayList<>());
        for(int i=0;i<nums.length;i++){
        int n=num.size();
        for(int j=0;j<n;j++){
            List<Integer> k=new ArrayList<>(num.get(j));
            k.add(nums[i]);
            num.add(K);
        }
        }
        return num;
    }
}
