#include<stdio.h>
#include<math.h>
int main()
{
  int i,j,n;
  int sum=0;
  int ss=0;
  printf("one shu:");
  scanf_s("%d",&n0;
  for(i=0;i<5;i++)
  {
     j=n*pow(10,i);
     sum+=j;
     ss+=sum;
  }
  printf("%d",ss);
  return 0;
}
